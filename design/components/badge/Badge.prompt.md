Small uppercase mono pill for statuses and tags; add `dot` for a live/active indicator.

```jsx
<Badge dot>Active</Badge>
<Badge>Example integrations</Badge>
```

Keep labels 1–2 words. Color is always silver — never introduce a status hue.
